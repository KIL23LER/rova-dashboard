{
  "buildCommand": "npm install -g pnpm && pnpm install && pnpm run build:dashboard",
  "outputDirectory": "artifacts/dashboard/dist/public",
  "installCommand": "echo 'install handled in build'",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
